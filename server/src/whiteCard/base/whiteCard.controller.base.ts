/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { WhiteCardService } from "../whiteCard.service";
import { WhiteCardCreateInput } from "./WhiteCardCreateInput";
import { WhiteCardWhereInput } from "./WhiteCardWhereInput";
import { WhiteCardWhereUniqueInput } from "./WhiteCardWhereUniqueInput";
import { WhiteCardFindManyArgs } from "./WhiteCardFindManyArgs";
import { WhiteCardUpdateInput } from "./WhiteCardUpdateInput";
import { WhiteCard } from "./WhiteCard";
import { RoundFindManyArgs } from "../../round/base/RoundFindManyArgs";
import { Round } from "../../round/base/Round";
import { RoundWhereUniqueInput } from "../../round/base/RoundWhereUniqueInput";
@swagger.ApiBearerAuth()
export class WhiteCardControllerBase {
  constructor(
    protected readonly service: WhiteCardService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: WhiteCard })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: WhiteCardCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<WhiteCard> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "WhiteCard",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"WhiteCard"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        deck: data.deck
          ? {
              connect: data.deck,
            }
          : undefined,

        players: data.players
          ? {
              connect: data.players,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        deck: {
          select: {
            id: true,
          },
        },

        discoarded: true,
        id: true,

        players: {
          select: {
            id: true,
          },
        },

        selected: true,
        text: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [WhiteCard] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(WhiteCardFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<WhiteCard[]> {
    const args = plainToClass(WhiteCardFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "WhiteCard",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        deck: {
          select: {
            id: true,
          },
        },

        discoarded: true,
        id: true,

        players: {
          select: {
            id: true,
          },
        },

        selected: true,
        text: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: WhiteCard })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: WhiteCardWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<WhiteCard | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "WhiteCard",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        deck: {
          select: {
            id: true,
          },
        },

        discoarded: true,
        id: true,

        players: {
          select: {
            id: true,
          },
        },

        selected: true,
        text: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: WhiteCard })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: WhiteCardWhereUniqueInput,
    @common.Body()
    data: WhiteCardUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<WhiteCard | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "WhiteCard",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"WhiteCard"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          deck: data.deck
            ? {
                connect: data.deck,
              }
            : undefined,

          players: data.players
            ? {
                connect: data.players,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          deck: {
            select: {
              id: true,
            },
          },

          discoarded: true,
          id: true,

          players: {
            select: {
              id: true,
            },
          },

          selected: true,
          text: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: WhiteCard })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: WhiteCardWhereUniqueInput
  ): Promise<WhiteCard | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          deck: {
            select: {
              id: true,
            },
          },

          discoarded: true,
          id: true,

          players: {
            select: {
              id: true,
            },
          },

          selected: true,
          text: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/rounds")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(RoundFindManyArgs)
  async findManyRounds(
    @common.Req() request: Request,
    @common.Param() params: WhiteCardWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Round[]> {
    const query = plainToClass(RoundFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Round",
    });
    const results = await this.service.findRounds(params.id, {
      ...query,
      select: {
        blackCard: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        czar: {
          select: {
            id: true,
          },
        },

        game: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,

        winningCard: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/rounds")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "update",
    possession: "any",
  })
  async createRounds(
    @common.Param() params: WhiteCardWhereUniqueInput,
    @common.Body() body: WhiteCardWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      rounds: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "WhiteCard",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"WhiteCard"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/rounds")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "update",
    possession: "any",
  })
  async updateRounds(
    @common.Param() params: WhiteCardWhereUniqueInput,
    @common.Body() body: RoundWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      rounds: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "WhiteCard",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"WhiteCard"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/rounds")
  @nestAccessControl.UseRoles({
    resource: "WhiteCard",
    action: "update",
    possession: "any",
  })
  async deleteRounds(
    @common.Param() params: WhiteCardWhereUniqueInput,
    @common.Body() body: WhiteCardWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      rounds: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "WhiteCard",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"WhiteCard"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
